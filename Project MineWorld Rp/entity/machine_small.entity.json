{
   "format_version": "1.10.0",
   "minecraft:client_entity": {
      "description": {
         "identifier": "mineworld:machine_small",
         "materials": { "default": "spider" },
         "textures": { "default": "textures/entity/machine/small" },
         "geometry": { "default": "geometry.allie_machine" },
         "scripts": {
            "pre_animation":[
               "v.unWeapon = ( q.mark_variant == 0 ) ? 1 : 0;",
               "v.shield = ( q.mark_variant == 1 ) ? 1 : 0;",
               "v.turret = ( q.mark_variant == 2 ) ? 1 : 0;",
               "v.shieldTurret = ( q.mark_variant == 3 ) ? 1 : 0;"
            ],
            "animate": [ "look_at_target" ]
         },
         "animations": {
            "look_at_target": "animation.machine.look_at_target",
            "walk": "animation.machine_small.walk"
         },
         "render_controllers": [ "controller.render.machine_small" ],
         "spawn_egg": {
            "texture": "mineworld_spawn",
            "texture_index": 0
         }
      }
   }
}
